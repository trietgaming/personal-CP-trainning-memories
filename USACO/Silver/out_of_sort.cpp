#include <bits/stdc++.h>

using namespace std;

const string FNAME = "sort";

int main() {

  freopen((FNAME + ".in").c_str(), "r", stdin);
//  freopen((FNAME + ".out").c_str(), "w", stdout);

  int n;
  cin >> n;

  for (int i = 0; i < n; ++i) {
    cin >> arr[i];
  }

  return 0;
}


